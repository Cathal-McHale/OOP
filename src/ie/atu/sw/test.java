package ie.atu.sw;

import java.util.Arrays;

public class test //implements //Item 1
{
	public enum Level {PRO, HOBBY};
	private char[] name;
	private localDate date;
	private List<Activity> activites;
	
	public activityContainer(String s, List<Activity>) list){
		name = Arrays.copyOf(s.toCharArray(), s.length());
		activites = new ArrayList<>(list);
	}
	public 

}
